import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calendar-migration-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
